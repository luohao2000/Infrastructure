spring:
  cloud:
    kubernetes:
      config:
        sources:
#          - name: application
#          - name: alarm-probe
#        namespace: mp-sre

log:
  appender:
    kafka:
      host: 10.238.239.200:9092
    scenario: ${product.line}
logging:
  config: classpath:logback-itiaoling.xml